//Creamos variable. 
//Definir una variable es darle un nombre a la variable (let nombre)
let nombre = 'Hola mundo';

//Modos de nombrar varibles (conveciones).
//UpperCamelCase. Esto suele usarse en algunos lenguajes para nombrar constantes (const).
let NombreCompleto;

//camelCase. Esta es la manera más usada para las variables en JS.
let nombreCompleto;

//snake_case.
let nombre_completo;

console.log(nombre);

//Podemos definir una variable y no darle valor.
//Definimos variable
let apellido;
//Añadimos valor a la variable.
apellido = 'Fonte';
//Podemos definir más de una variable a la vez.
let calle, numero, piso;
